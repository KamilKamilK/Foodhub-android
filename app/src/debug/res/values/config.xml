<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Debug build only: local Docker foodhub-api instead of the placeholder
         deployment host in core/network's main config.xml. 10.0.2.2 is the
         Android emulator's alias for the host machine's loopback interface. -->
    <string name="foodhub_api_base_url" translatable="false">http://10.0.2.2:8000/</string>
    <!-- Debug build only: local Docker Mercure hub instead of the placeholder
         deployment host in core/realtime's main config.xml. -->
    <string name="foodhub_mercure_url" translatable="false">http://10.0.2.2:8090/.well-known/mercure</string>
</resources>
